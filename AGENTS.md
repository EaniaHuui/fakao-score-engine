# 法考 AI 提分工具箱公开仓库规则

## 目标

本仓库公开可复用的提分工具和方法，不公开个人数据、平台凭据、未经授权的题库或课程。

## 允许提交

- CLI、Skills、数据结构、空白模板；
- 自制示例题和匿名化样例；
- 不含真实凭据的测试；
- 安装和使用文档。

## 禁止提交

- 竹马 cookie/token、夸克 cookie、API Key；
- 竹马导出文件、完整真题、题库解析、课程讲义；
- 用户档案、错题、作答记录、学习计划和生成报告；
- 绝对本机路径、个人联系方式和运行时产物。

## 数据处理

用户资料只能进入 `01_待导入资料`，原始资料和运行结果不应提交到公开仓库。AI 生成内容必须标注来源和审核状态。

## 验证

提交前执行：

```bash
./fakao --help
python3 -m compileall -q 09_导入与处理工具/cli
rg -n "token|cookie|sk-|/Users/|Q-" .
```

搜索结果必须人工确认没有真实凭据、个人路径或运行时数据。
