# fakao-planner

## 目标

在剩余时间和每日容量约束下，安排预期提分收益最高的有限任务。

## 排序

`考点分值权重 × 近年频率 × 用户错误率 × 可提升程度 ÷ 预计耗时`。

必须优先处理到期复测、高收益错题和猜对高风险题；低频、争议大、耗时长的内容自动降级。每日输出不超过用户可用时间的 80%，保留中断缓冲。

## 输出

写入 `06_提分任务/今日任务`，每项任务包含考点/题目 ID、预计分钟数、选择理由、验收标准和失败后的重排规则。
