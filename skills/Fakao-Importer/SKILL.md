# fakao-importer

## 目标

把用户课程、讲义、笔记和题库转成可审核的原始资料与结构化题目，不改变原文。

## 流程

1. 扫描文件并识别格式。
2. 提取章节、题干、选项、答案、解析和来源位置。
3. 去重并标记缺失字段、解析失败、来源不明和可能过时的内容。
4. 先写入 `01_待导入资料` 或结构化待审核结果，用户确认后再进入正式库。

## 约束

任何题目和规则都必须保留 `source_file`、法律版本和 `审核状态`。不把 AI 推断当成标准答案。
