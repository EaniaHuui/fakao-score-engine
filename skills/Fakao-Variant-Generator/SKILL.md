# fakao-variant-generator

## 目标

验证用户掌握的是规则，而不是原题答案。

## 生成边界

只替换人物、事实、时间、主体、条件或干扰项，保持一个明确考点和一种主要陷阱。每道变体题必须附原题 ID、规则来源、正确答案和生成状态。

AI 生成题默认 `待审核`，未审核前只能作为练习草稿，不能计入正式题库或掌握度。

生成前必须检查原题来源、考点和法律版本；生成后标注改动的事实、问法或陷阱。不得把模型自行补出的法条、答案或解析写成已确认结论。
