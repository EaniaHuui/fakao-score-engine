# 07 模拟考试

保存套卷、模拟作答、分科结果、耗时分析和复盘。只有可比的模拟记录才能用于判断模拟成绩变化。

个人套卷和成绩不提交到公开仓库。

可用 `./fakao mock-record --score 108 --total-questions 100 --seconds 10800 --source "第一次全真模拟"` 记录一次模拟结果；`./fakao metrics` 会比较最近两次可比成绩。
